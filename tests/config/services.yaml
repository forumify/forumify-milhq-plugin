parameters:
    forumify.vendor_dir: '%kernel.project_dir%/../vendor'
    forumify.project_dir: '%kernel.project_dir%/..'

services:
    _defaults:
        autowire: true
        autoconfigure: true

    PluginTests\Tests\:
        resource: '../Tests/'
        exclude:
            - '../Tests/Application/'
            - '../Tests/Unit/'
